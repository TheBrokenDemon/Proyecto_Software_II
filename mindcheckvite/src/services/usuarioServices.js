// Este es el archivo que permite conectar backend con frontend (registro de usuario)

// API_URl a postman
const API_URL = "http://localhost:3000";

export const obtenerRegistro = async (registro) => {
    try {
        
    }
    catch (error) {
        console.error("Error al obtener registro de usuario", error);
    }
}
